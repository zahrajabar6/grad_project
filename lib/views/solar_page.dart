import 'package:flutter/material.dart';

class SolarPage extends StatelessWidget {
  const SolarPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      body: Center(
        child: Text('Solar', style: TextStyle(fontSize: 20),),
      ),
    );
  }
}
